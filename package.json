{
  "scripts": {
    "start": "npm-run-all --parallel start:server start:client",
    "start:server": "cd backend && npm start dev",
    "start:client": "cd frontend && npm start"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
